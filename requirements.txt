# Binary Vulnerability Scanner and Fuzzer - Requirements
# Python 3.7+

# ============================================
# REQUIRED Python Packages
# ============================================
# scanner.py only uses pwntools as external module
# All other imports (tkinter, subprocess, os, etc.) are from Python standard library

pwntools>=4.8.0  # Essential for binary exploitation and analysis

# ============================================
# OPTIONAL Python Packages
# ============================================
# Enhanced functionality - scanner works without these

ropper>=1.13.8  # For advanced ROP gadget finding (fallback to objdump if not installed)

# ============================================
# System Dependencies (Required)
# ============================================
# The following external tools must be installed on your system:
# - file (Linux: apt-get install file, macOS: brew install file)
# - readelf (Linux: apt-get install binutils, macOS: brew install binutils)
# - objdump (Linux: apt-get install binutils, macOS: brew install binutils)
# - nm (Linux: apt-get install binutils, macOS: brew install binutils)
# - strings (Linux: apt-get install binutils, macOS: brew install binutils)
# - checksec (optional: apt-get install checksec)
# - gdb (optional: apt-get install gdb, macOS: brew install gdb)

# ============================================
# Standard Library Modules Used (No Installation Needed)
# ============================================
# tkinter - GUI framework (comes with Python, may need: apt-get install python3-tk)
# subprocess, os, time, threading, struct, re, json, tempfile, random, 
# signal, hashlib, datetime, platform, sys, typing, traceback
